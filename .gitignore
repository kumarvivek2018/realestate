# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Data — by default we DO commit data so re-fetching isn't needed.
# Block only the noisy/big/redundant files.
data/raw/pf_listings_*.jsonl    # redundant: same data is in the .parquet
data/raw/_*                     # debug screenshots, captured creds, samples
data/raw/*.png

# Notebook scratch
.ipynb_checkpoints/
*.ipynb_meta

# OS
.DS_Store

# IDE
.vscode/
.idea/
